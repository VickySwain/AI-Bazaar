apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: '15s'
      queryTimeout: '30s'
      httpMethod: POST

  - name: PostgreSQL
    type: postgres
    url: postgres:5432
    database: coverai_db
    user: coverai
    secureJsonData:
      password: coverai_secret
    jsonData:
      sslmode: disable
      maxOpenConns: 5
      maxIdleConns: 2
      postgresVersion: 1600
      timescaledb: false
    editable: false
